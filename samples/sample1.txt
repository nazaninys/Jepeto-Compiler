
func maxfinder(arr,i,equal,max):
     if arr.size < i:
         return max


     else :
       maxfinder(arr,i+1,equal,equal(arr[i],arr[i+1]));


main: maxfinder(arr=[1,2,3,4,5],i=0,(a,b)->{if a>b: return a; else: return b; });

